export default class RecipeProperty {
    key;
    label;
    sourceList;

    constructor(key, label, sourceList) {
        this.key = key;
        this.label = label;
        this.sourceList = sourceList;
    }
}